# EHW-3.0 Results — Host Oracle For Spare-Routing Recovery

Generated by:

```bash
python3 sim/oracle_spare_routing.py
```

Status: **PASS — host-only**. No board claim is made for this rung. Current
default population is `128`, chosen so the later NEORV32 firmware fits the fixed
16 KiB DMEM.

## Substrate

- Target function: 3-input majority, truth mask `0xe8`, max fitness `8/8`.
- Fixed outer route: source pool `P=[x0,x1,x2,ZERO,ONE]`, four C1 nodes
  `A0/A1/A2/AS`, and one output node `O`.
- C1 nodes are 2-input LUT4s. `O` is a 3-input LUT8.
- Evolved fields are only LUT INITs and local mux selects:
  `logic_init[0..3]`, `init_out`, `node_sel[0..3][0..1]`, `out_sel[0..2]`.
- The physical route is fixed. No raw Xilinx routing bits are mutated.

## Validity Layer

Every mux is a pure fan-in selector, not a bus, so every genome decodes to a
single-driver circuit. Invalid selector bytes decode to harmless defaults:

- `node_sel >= 5 -> ZERO`
- `out_sel >= 4 -> A0`

The fault model is injected during evaluation and is not part of the genome.
These are modeled island faults, not physical destruction of FPGA resources.

## Host Run

```text
representability_self_check=PASS target=0xe8 output_lut=LUT8
validity_layer=PASS pure fan-in muxes; node_sel>=5->ZERO; out_sel>=4->A0

== no-fault champion ==
fault=FAULT_NONE
generation=21
fitness=8/8
mask=0xe8 target=0xe8
genome=0a 08 01 0f 32 01 04 00 02 02 00 04 01 01 02 00
A0 = LUT4(init=0xa, in0=x1, in1=ONE)
A1 = LUT4(init=0x8, in0=x0, in1=x2)
A2 = LUT4(init=0x1, in0=x2, in1=x0)
AS = LUT4(init=0xf, in0=ONE, in1=x1)
O  = LUT8(init=0x32, in0=A1, in1=A2, in2=A0)
row x2x1x0 out gold
  0   000    0   0
  1   001    0   0
  2   010    0   0
  3   011    1   1
  4   100    0   0
  5   101    1   1
  6   110    1   1
  7   111    1   1

== injected fault on no-fault champion ==
fault=FAULT_DISABLE_NODE(A1)
fitness=7/8
mask=0xc8 target=0xe8

== post-fault repaired champion ==
fault=FAULT_DISABLE_NODE(A1)
generation=17
fitness=8/8
mask=0xe8 target=0xe8
genome=0b 09 09 03 b1 00 04 04 01 02 00 00 01 02 03 00
A0 = LUT4(init=0xb, in0=x0, in1=ONE)
A1 = LUT4(init=0x9, in0=ONE, in1=x1)
A2 = LUT4(init=0x9, in0=x2, in1=x0)
AS = LUT4(init=0x3, in0=x0, in1=x1)
O  = LUT8(init=0xb1, in0=A2, in1=AS, in2=A0)
row x2x1x0 out gold
  0   000    0   0
  1   001    0   0
  2   010    0   0
  3   011    1   1
  4   100    0   0
  5   101    1   1
  6   110    1   1
  7   111    1   1

== field-level repair diff ==
logic_init[A0]: 0xa -> 0xb
logic_init[A1]: 0x8 -> 0x9
logic_init[A2]: 0x1 -> 0x9
logic_init[AS]: 0xf -> 0x3
init_out[O]: 0x32 -> 0xb1
node_sel[A0][in0]: 1(x1) -> 0(x0)
node_sel[A1][in0]: 0(x0) -> 4(ONE)
node_sel[A1][in1]: 2(x2) -> 1(x1)
node_sel[AS][in0]: 4(ONE) -> 0(x0)
out_sel[O][in0]: 1(A1) -> 2(A2)
out_sel[O][in1]: 2(A2) -> 3(AS)
physical_routing: unchanged (fixed-route island; genome contains only INIT/select fields)

summary=PASS no_fault=8/8 degraded<8 repaired=8/8 repaired_mask=0xe8
```

## Interpretation

The no-fault genome implements the target majority mask `0xe8`. When
`FAULT_DISABLE_NODE(A1)` is injected, the same genome drops to `7/8` with mask
`0xc8`, proving the injected fault affects the live phenotype.

The recovery run keeps the fault active and evolves a new genome that reaches
`8/8` again. The repaired output mux no longer selects `A1`; it selects `AS` as
one of the three inputs, so the repair uses the spare node and local rerouting
inside the fixed-route island.

This rung established the host-side decode contract for EHW-3.1. The Python/C
bit-exact twin has since been added in `docs/ehw3_1_results.md`.
